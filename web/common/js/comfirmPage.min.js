(function() {
	var bindData={comfirm:function(){
		        		$.post('setWarningSession',{},function(data){
		        			if(location.search){
		        				var url=location.search.replace('?page=','');
		        				location.href=url;
		        			}
		        		});
		        	}
		        };
	$.bindData.compileAll($('#footer'),bindData); 
})()